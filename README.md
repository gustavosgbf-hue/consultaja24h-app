# ConsultaJá24h App

Aplicativo mobile do paciente para iOS e Android.

Primeiro marco técnico: autenticação com a API atual, restauração segura da sessão, `/api/paciente/me` e `/api/paciente/agendamentos`, sem alterar os fluxos de produção existentes.
